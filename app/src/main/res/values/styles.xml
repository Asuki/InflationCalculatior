<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimaryInverse">#FF2</item>
    </style>

    <style name="AppMain" parent="AppTheme">
        <item name="android:layout_height">@dimen/size</item>
        <item name="android:padding">@dimen/padding</item>
    </style>

    <style name="Button" parent="AppMain">
        <item name="android:background">@drawable/button_bg</item>
        <item name="android:tint">@color/colorPrimary</item>
        <item name="android:layout_width">@dimen/size</item>
        <item name="android:layout_marginRight">10dp</item>
        <item name="android:layout_marginLeft">10dp</item>
    </style>

    <style name="Text" parent="AppMain">
        <item name="android:textSize">20sp</item>
        <item name="android:layout_marginLeft">12dp</item>
        <item name="android:layout_marginRight">12dp</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="ListItems" parent="AppMain">
        <item name="android:textSize">16sp</item>
        <item name="android:layout_marginStart">5dp</item>
    </style>

</resources>
